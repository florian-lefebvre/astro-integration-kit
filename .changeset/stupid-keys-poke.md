---
"astro-integration-kit": minor
---

Allows passing imports as an array to `addVirtualImports` for more advanced use cases (eg. for different versions on server and client). This is a breaking change when using it as a utility as it now requires a new `config` prop
